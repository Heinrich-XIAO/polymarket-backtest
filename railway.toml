# Railway monorepo — configure Root Directory per service in Railway dashboard:
#   backend service   → Root Directory: backend/
#   celery-worker     → Root Directory: backend/  (start: celery -A tasks worker --loglevel=info)
#   frontend service  → Root Directory: frontend/
#
# Each service uses its own railway.json from its subdirectory.
