# Railway monorepo — configure Root Directory per service in Railway dashboard:
#   backend  → Root Directory: backend/  (start: uv run uvicorn main:app --host 0.0.0.0 --port $PORT)
#   frontend → Root Directory: frontend/
