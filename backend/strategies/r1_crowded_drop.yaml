name: R1 Crowded Drop
description: Drop amplified by sector crowding — more participants, faster reversion.

entry:
  condition: price_drop_pct * (1 + concentration / 10) > 0.15
  lookback_days: 7

exit:
  take_profit: 0.85
  stop_loss: 0.80

filters:
  min_volume: 5000
  categories: [politics, crypto, economics, sports]
  max_days_to_resolution: 9999

stake_pct: 0.04
