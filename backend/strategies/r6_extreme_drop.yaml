name: R6 Extreme Drop
description: Massive drop regardless of other factors — panic entry.

entry:
  condition: price_drop_pct > 0.25
  lookback_days: 7

exit:
  take_profit: 0.85
  stop_loss: 0.80

filters:
  min_volume: 5000
  categories: [politics, crypto, economics, sports]
  max_days_to_resolution: 9999

stake_pct: 0.05
