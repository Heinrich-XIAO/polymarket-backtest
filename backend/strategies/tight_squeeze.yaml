name: Tight Squeeze
description: Tight range with volume spike and drop — compressed breakout reversal.

entry:
  condition: price_range < 0.06 and price_drop_pct > 0.06 and volume_zscore > 1.0
  lookback_days: 7

exit:
  take_profit: 0.85
  stop_loss: 0.80

filters:
  min_volume: 3000
  categories: [politics, crypto, economics, sports]
  max_days_to_resolution: 9999

stake_pct: 0.04
