name: R5 Young & Concentrated
description: New market in a crowded sector — noisy, prone to overreaction.

entry:
  condition: market_age_days < 20 and concentration > 3 and price_drop_pct > 0.10
  lookback_days: 5

exit:
  take_profit: 0.85
  stop_loss: 0.80

filters:
  min_volume: 3000
  categories: [politics, crypto, economics, sports]
  max_days_to_resolution: 9999

stake_pct: 0.03
