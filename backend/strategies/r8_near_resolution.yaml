name: R8 Cheap NO Fade
description: Buy NO when YES is near 0.9 with high volume — fading overconfidence.

entry:
  condition: price > 0.85 and volume_zscore > 1.0 and price_volatility > 0.02
  lookback_days: 5

exit:
  take_profit: 0.50
  stop_loss: 0.60

filters:
  min_volume: 3000
  categories: [politics, crypto, economics, sports]
  max_days_to_resolution: 9999

position_side: "no"
stake_pct: 0.03
