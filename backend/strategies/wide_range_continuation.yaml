name: Wide Range Continuation
description: Wide-ranging market with a continuation drop — trend is strong.

entry:
  condition: price_range > 0.15 and price_drop_pct > 0.10 and volume_zscore > 0.8
  lookback_days: 10

exit:
  take_profit: 0.85
  stop_loss: 0.80

filters:
  min_volume: 5000
  categories: [politics, crypto, economics, sports]
  max_days_to_resolution: 9999

stake_pct: 0.04
